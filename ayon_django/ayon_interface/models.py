from django.db import models
from django.contrib.auth.models import User

# We're not defining custom models here as we'll be using the AYON API directly.
# This file is included for Django's app structure completeness.